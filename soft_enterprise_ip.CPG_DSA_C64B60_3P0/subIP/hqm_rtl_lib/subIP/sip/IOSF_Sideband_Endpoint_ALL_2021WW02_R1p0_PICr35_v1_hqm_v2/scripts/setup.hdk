#!/usr/intel/bin/tcsh -f
#
####################################################
# source scripts/setup for HDK 
####################################################

source /p/hdk/rtl/hdk.rc -cfg sip -reentrant

if ($status) then
    echo "HDK setup failed... please check your access for the following command!!!"
    echo "% source /p/hdk/rtl/hdk.rc -cfg sip -reentrant"
else
    echo "Now you can run simbuild/febe commands!"
endif

