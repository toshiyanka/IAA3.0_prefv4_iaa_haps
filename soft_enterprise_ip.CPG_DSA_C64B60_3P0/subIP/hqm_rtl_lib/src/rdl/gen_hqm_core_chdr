osetup nebulon
setenv PATH ${NEBULON_DIR}/bin:${PATH}

nebulon -input hqm_core.rdl -xml -cspec -chdr -I "$NEBULON_DATA/include . ../../subIP/sip/AW/src/rdl" -out_dir chdr_out

if ($status != 0) then
  echo "$0 Error: hqm_core.rdl nebulon command failed"
  exit
endif
