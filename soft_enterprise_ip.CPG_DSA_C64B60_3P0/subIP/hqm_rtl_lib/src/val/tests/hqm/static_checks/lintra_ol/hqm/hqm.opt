{
 %opts = (
# replace with appropriate values
    -static_check_top => "hqm_rtl_lib.hqm",

    -model => "hqm",

    -static_check_tools  => [ "lintra", ],

    # If you want to override the default static_check_cfg_file defined in _local_ivars.udf for this model
#    -static_check_cfg_file => [ "$ENV{MODEL_ROOT}/tools/openlatch/hqm/lintra_static_check.cfg", ],

    # Add other lintra run options. Use `ace -sh LINTRA`
    -lintra_goal     =>  "openlatch", # or use -lintra_goalsets -lintra_use_goalsets #

    -post_process_pattern_file => "$ENV{ACE_HOME}/udf/ace_base_postsim.pp",
  );
}
