
VC App: cmpTwoVdbTgl.pl

Usage: 
  cmpTwoVdbTgl.pl -vcapp_lib <vcapp_shared_lib> -vdb1 <the coverage database <dir>> -vdb2 <the coverage database <dir>> [-scope <scope name>] [-only_port] [-elfile <exclude_file>] [-o <output file>] 
  Arguments: 
    -vcapp_lib <path>/libCmpTwoVdbTgl.so:  specify the VC App shared lib. 
    -vdb1     <dir>                        the first coverage database in <dir>. 
    -vdb2     <dir>                        the second coverage database in <dir>. 
    -scope    <scope name>                 only check the scope and its sub scopes. 
    -only_port                             when specify this option, only check ports. 
    -elfile   <exclusion file>             Exclusion file to be loaded. 
    -o        <output file>                If not specified, default is comp_result.log. 

  Ex: cmpTwoVdbTgl.pl -vcapp_lib ./libCmpTwoVdbTgl.so -vdb1 simv1.vdb -vdb2 simv2.vdb -scope tb_top 


Environment Setting:
  1. set $VERDI_HOME
       setenv VERDI_HOME /global/apps5/verdi_2017.12
