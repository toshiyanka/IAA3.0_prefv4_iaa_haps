use lib "$ENV{MODEL_ROOT}/cfg/ace/lib";
use common::RunModes;
common::RunModes::init_library;
{
   %opts = (
      -simv_args => [
            "+AW_CONTINUE_ON_ERROR", "+iosf_tracker_filename=iosf_trk", "-assert nopostproc", "+vcs+lic+wait", "+SLA_FLUSH_PHASE_TIMEOUT=400000", "+SLA_MAX_RUN_CLOCK=8000000",
            ],
      -test_in_model => 1,
      -enabled_post_process_modes => "hqm_test",
   );
   %runModes = (
      sec_2_1_1_3      => { 
          -simv_args => ["",],
          },
      rd_then_wr => { 
          -simv_args => [ "+HQM_CFG_RD_BEFORE_WR_SEQ ", " ",  ],
          },
      lcp_shift_check => { 
          -simv_args => [ "+LCP_SHIFT_CHECK ", ], 
          },
      unimp_func => { 
          -simv_args => [ "+HQM_UNIMP_FUNC_ACCESS", "+eot_pf +pcie_urd +pcie_anfes +pcie_ced +EXP_UNWANTED_TXN_TO_HQM",  ],
          },
      unimp_regs => {
          -simv_args => [ "+HQM_UNIMP_CFG_SPACE_ACCESS", "+SLA_CONFIG_PHASE_TIMEOUT=500000000 +SLA_PCIE_FLUSH_PHASE_TIMEOUT=30000000",  "+OVM_GLOBAL_TIMEOUT=30000ms", ], 
          },
   );
   import_runmodes (
         -dest_hash => \%runModes,
   );
}

