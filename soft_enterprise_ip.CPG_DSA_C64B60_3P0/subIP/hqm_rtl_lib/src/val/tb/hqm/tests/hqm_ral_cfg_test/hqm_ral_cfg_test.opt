use lib "$ENV{MODEL_ROOT}/cfg/ace/lib";
use common::RunModes;
common::RunModes::init_library;
{
   %opts = (
      -simv_args => ["+AW_CONTINUE_ON_ERROR", 
                     "+iosf_tracker_filename=iosf_trk",
                     "-assert nopostproc",
                     "+vcs+lic+wait",
                     "+HQM_PCIE_RC_VIP_PRESENT",
                     "+SLA_FLUSH_PHASE_TIMEOUT=10000000",
                     "+SLA_USER_DATA_PHASE_TIMEOUT=200000",
                     "+SLA_MAX_RUN_CLOCKS=10000000",
                    ],
      -test_in_model => 1,
      -enabled_post_process_modes => "hqm_test",
   );
   %runModes = (
      hqm_resource_reg_check  => { -simv_args => ["",],  },

   );
   import_runmodes (
         -dest_hash => \%runModes,
   );
}

