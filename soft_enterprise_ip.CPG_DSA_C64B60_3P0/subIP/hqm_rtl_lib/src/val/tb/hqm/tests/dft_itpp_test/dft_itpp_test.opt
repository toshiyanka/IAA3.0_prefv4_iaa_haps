use lib "$ENV{MODEL_ROOT}/cfg/ace/lib";
use common::RunModes;
common::RunModes::init_library;
{
    %opts = (
        -enabled_post_process_modes => 'dft_itpp',
        -simv_args => [
              "+OVM_TESTNAME=$ENV{TESTNAME}",
            ],
    );
}
