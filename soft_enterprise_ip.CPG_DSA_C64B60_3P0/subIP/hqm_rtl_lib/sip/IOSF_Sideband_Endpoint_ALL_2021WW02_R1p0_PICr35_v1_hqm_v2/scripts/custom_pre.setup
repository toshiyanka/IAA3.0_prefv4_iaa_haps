

if (-e "setup") then
   setenv SBR_ROOT `realpath ../` 
   setenv IP_ROOT  `realpath ../`
   setenv MODEL_ROOT  `realpath ../`
   setenv FE2BE_ROOT  `realpath ../`
   setenv REPO_ROOT  `realpath ../`
else if (-e "scripts/setup") then
   setenv SBR_ROOT `realpath ./`
   setenv IP_ROOT  `realpath ./`
   setenv MODEL_ROOT  `realpath ./`
   setenv FE2BE_ROOT  `realpath ../`
   setenv REPO_ROOT  `realpath ../`
endif

setenv SIP_PROJECT sb
